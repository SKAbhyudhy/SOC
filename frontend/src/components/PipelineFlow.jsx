import React from 'react';
export default function PipelineFlow(){return <section className='panel'>Ingestion → Detection → LLM/RAG → MITRE → Intel → Risk → Playbook → Validation → Sandbox → Case → SLA → Audit</section>}
